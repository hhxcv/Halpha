interface:
  display_name: "Halpha Development"
  short_description: "Handle one Halpha scope with staged validation"
  default_prompt: "Use $develop-halpha to handle one authorized Halpha construction-package slice or explicit isolated non-package scope, choose the builder, observer, or finalizer work mode, and escalate validation only as required."
