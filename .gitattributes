# Repository text is stored and checked out with canonical LF on every host.
* text=auto eol=lf

# Markdown specification metadata uses two trailing spaces for intentional hard breaks.
docs/**/*.md whitespace=-blank-at-eol
