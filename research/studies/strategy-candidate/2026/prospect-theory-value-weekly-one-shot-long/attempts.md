# 尝试、失败与修正记录

## 2026-07-22 选题与冻结前查重

1. 查重现有策略/预测目录：价格趋势、动量/反转、MAX、高低波动、funding premium/discount、BTC lead-lag/residual、类别动量、52 周高点与 1m 订单失衡均已有结果；不重复近邻。
2. 比较 skewness/downside risk、配对交易、PTV 与逐笔 10 秒订单流。日内高阶矩的论文经济量级接近日频零售成本；配对交易需要核心协调双腿；逐笔 10 秒版本维护/执行成本过高。PTV 的外部周频幅度、单腿可转换性和纯基础数据需求最符合当前决策价值。
3. PTV 仍可能只是输家、低偏度或高波动暴露，因此三者与市场长仓在查看本题输出前被设为硬门。论文宽市场/退市币优势不外推到当前固定流动永续。
4. 在查看任何 PTV 排名、交易或收益前固定对象、公式参数、时间间隔、top 比例、仓位、持有/冷却、成本、funding、资本门、阶段、基准、邻域、统计门、停止规则和支持上限。

## 2026-07-22 合成验证与 checkpoint

1. `py_compile` 通过；对称收益分布 PTV 为 `-0.0245492`，整体上移 1% 后为 `-0.0041073`，单调方向符合定义。
2. 概率权重在 0/1 边界精确为 0/1；VectorBT 与独立现金流误差 `1.734723475976807e-17`。
3. 在读取本题历史 PTV 排名或收益前生成 checkpoint `f995c7ee55aa5f1acc5710a1fe433fd6cc08521c96c87049250df1e79e1ee8aa`。

## 2026-07-22 development 数据与运行

1. 只读复用已校验的 Binance 公共缓存；没有读取产品数据库、凭据或运行配置，没有调用交易端点。
2. `prepare --stage development` 为 `PASS`：92 个周有至少 20 个可排名目标，最少为 20；其余周保持 `NO_ACTION`。
3. `analyze --stage development` 一次完成，无实现错误或经济规则修订。主规则 146 笔、89 个行动日期、16 个目标；证据摘要 `872fa631b81cd74e69d8c9700b6f97270d28a9c51d08618a89fd7147b6a881e2`。

## 2026-07-22 development 反证与停止

1. base/stress 扣门日期均值为 `-0.1492% / -0.2476%`；stress 四周 block-bootstrap 95% 区间 `[-1.0027%, +0.5798%]`。
2. 2022/2023 base 扣门均值为 `-0.9195% / +0.4247%`，符号翻转；日期组合 base 最大回撤 `-32.28%`。
3. PTV 相对 `LOW_MOM52`、`LOW_SKEW52`、`HIGH_VOL52` 的 base 均值差为 `-0.2547% / +0.0833% / +0.2867%`。相对三种简单规则的每日期最高收益包络为 `-1.1645%`，95% 区间 `[-1.6222%, -0.7072%]`，明确更差。
4. gross 相对市场长仓为 `-0.0586%`，95% 区间 `[-0.4541%, +0.3800%]`；PTV52 与 MOM52 的周中位 Spearman 为 `0.8362`，大部分排序信息是普通长期相对动量。
5. 三个邻域 stress 均值仅 PTV78 为正；仅 Layer-1 与 Payment 两类别为正；至少两笔目标中仅 `30.77%` 为正，最大正 PnL 目标占 `45.58%`。
6. development 门 `FAIL`，结论 `DOES_NOT_SUPPORT`。2024 evaluation 未打开，不改参考点/偏好参数/窗口/阈值，不生成 handoff，产品影响 `NONE`。
7. `validate` 为 `PASS`：6 个 JSON 内容摘要和 9 份逐笔 CSV SHA-256 均通过。
