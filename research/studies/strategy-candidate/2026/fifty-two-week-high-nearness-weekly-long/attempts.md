# 尝试、失败与修正记录

## 2026-07-22 选题前筛选

1. 查重现有 20 个新方法策略候选和 legacy 支持快照。趋势/TSMOM、短中期动量、赢家/输家、MAX、普通高低波动、funding premium/discount、BTC lead-lag/residual、类别动量与 1 分钟失衡均已有结果；不得换近邻参数重复搜索。
2. 联网核对 IVOL、signed jump、季节性、残差动量、52 周锚定及其反证。IVOL 与本地波动族重叠；季节性有近期不稳健反证；signed jump 需要 5 分钟且每日换仓，论文价值加权毛价差小于本题压力往返成本。
3. 选中 52 周高点接近度：同行评审结果新、外部毛价差相对大、周频和日线适合个人半自动计划。它与趋势相邻，因此预先把 MOM52 和同周市场长仓的均值及 bootstrap 下界设为硬门，而不是只看绝对盈利。
4. 在查看任何本题 Nearness52 排名、交易或收益前，固定目标、周六截止/周一行动、52 周定义、top decile、0.25x、7d、冷却、成本、funding、资本门、三个邻域、三个基准、顺序阶段、支持上限和全部失败条件。

## 2026-07-22 冻结与合成验证

1. `py_compile` 通过；合成单调价格路径的 `Nearness52` 在新高处精确为 `1.0`。
2. VectorBT 两单回放与独立现金流计算误差为 `1.734723475976807e-17`；funding 压力方向测试通过。
3. 在任何本题历史排名、逐笔收益或汇总输出前生成 `checkpoint.json`，内容摘要为 `d976e5dfd243e938d2a07a2732eaae2e83b7136375742cbe3195642235dfcf1f`。

## 2026-07-22 development 数据质量

1. 只读复用 Git 外公开 Binance 日线与 funding 缓存；预先核对父研究 manifest、文件字节数及 SHA-256。没有读取产品数据库、凭据或运行配置。
2. `prepare --stage development` 为 `PASS`。最少可排名目标数为 19；不足预注册 20 个的周保持全局 `NO_ACTION`，没有插值或替换目标。

## 2026-07-22 第一次 development 分析中止与 amendment-001

1. 第一次 `analyze` 已计算逐笔表，但在写入汇总证据前中止：带冷却的 MOM52 基准在部分主规则有仓周自然为 `NO_ACTION`，继承的比较器却要求每个主规则日期都有基准交易。
2. 没有删除日期、虚构交易或改信号。`amendment-001.json` 仅明确把 MOM52 的无仓周按真实计划现金收益 0 对齐；主规则、参数、样本、成本、门槛和结论规则均未改变。
3. amendment 链从冻结 `study.py` SHA-256 `0020f20d…` 到 `3b4c40c…`，并由执行器在重跑前验证。

## 2026-07-22 development 结果与停止

1. 确定性重跑得到 95 笔、50 个行动日期、20 个目标；证据摘要 `ccf632527a73efd8f5779458d6b8335983cd4668d857fabcdfab05d8ba2232f8`。
2. base / stress 扣除全计划资本门后的日期周均为 `-0.2247% / -0.3142%`；stress 四周 block-bootstrap 95% 区间为 `[-1.2287%, +1.0400%]`。
3. H1 / H2 base 扣门周均为 `-0.8759% / +0.4808%`，时间不稳定。
4. base 相对 MOM52 均值为 `+0.3062%`，但 95% 区间 `[-0.6841%, +1.6536%]`；gross 相对市场长仓为 `-0.3565%`，95% 区间 `[-0.9137%, +0.3799%]`。
5. 日期组合最大回撤 `-27.64%`；至少两次交易目标中仅 `30.77%` 为正；最大正 PnL 目标占 `53.06%`。
6. development 门为 `FAIL`，结论 `DOES_NOT_SUPPORT`。按顺序门不打开 evaluation，不搜索近邻，不生成 handoff；产品影响为 `NONE`。
7. `validate` 为 `PASS`：7 个 JSON 内容摘要与 7 份逐笔 CSV SHA-256 均验证，重演证据保留在本目录。
